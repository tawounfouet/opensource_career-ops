CAREER_OPS_ROOT=..

# Optional Django backend bridge. When set, supported Next.js API routes try
# Django first and fall back to their existing local Node implementation if the
# backend is unavailable.
# CAREER_OPS_API_URL=http://localhost:8000
